name: My GitHub Action

on:
  push:
    branches:
      - main

jobs:
  my_job:
    runs-on: ubuntu-latest

    steps:
      - name: Use API key
        env:
          API_KEY: ${{ secrets.API_KEY }}
        run: |
          # Access the secret using the $API_KEY environment variable
          echo $API_KEY
